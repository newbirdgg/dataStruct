##什么是Trie
* 如果有n个条目

|普通树状结构|Trie|
|:---:|:---:|
|查询的时间复杂度是*O(logn)*|查询每个条目的时间复杂度和字典中一共多少条目无关|
|如果有100万个条目，logn大约是20|时间复杂度为O(W),W为查询单词的长度，大多数单词长度小于10|

* 简单的字母trie每个节点有26个指向下个节点的指针
* 有时候需要考虑不同的语言，不同的语境。
* 所以每个节点有若干个指向下个节点的指针
```
class Node{
    //判断当前节点是否代表一个单词的结尾
    boolean isWord
    Map<char,Node> next;
}
```
