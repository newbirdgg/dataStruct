###HashMap
* 容量(capacity)： 表中的桶位数
    - 散列表的槽位通常称为桶位
* 初始容量：表在创建时所拥有的桶位数。HashMap和HashSet都具有允许你指定初始容量的构造器
* 尺寸(size)：表中当前存储的项数
* 负载因子(load factor): 等于（尺寸 / 容量），空表的负载因子是0，半满表的负载因子是0.5。
负载轻的表产生冲突的可能性小，因此对于插入和查找都是理想操作（但是会减慢迭代器遍历的过程），
当负载情况达到负载因子的水平时，容器将自动增加其容量（桶位数），实现方式是使容量大致加倍，
并重新将现有对象分布到新的桶位集中（这称为再散列）
* HashMap的默认负载因子是0.75，这个因子在时间和空间代价之间达到平衡。    